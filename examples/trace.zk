circuit trace_demo {
    public expected: field;
    private a: field;
    private b: field;

    let product = a * b;
    let shifted = product + 9;
    let balance = shifted - a;

    constrain balance == expected;
    expose product;
    expose balance;
}
